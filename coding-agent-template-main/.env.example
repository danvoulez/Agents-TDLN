# ============================================
# ATOMIC AGENTS BACKEND
# ============================================

# URL do backend Atomic Agents (Worker)
# Em desenvolvimento local, use: http://localhost:8000
# Em produção, aponte para o servidor do Worker
ATOMIC_API_URL=http://localhost:8000

# ============================================
# GITHUB OAUTH (para autenticação de usuários)
# ============================================

# Crie um GitHub OAuth App em: https://github.com/settings/developers
# Callback URL deve ser: http://localhost:3000/api/auth/github/callback (dev)
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
GITHUB_CALLBACK_URL=http://localhost:3000/api/auth/github/callback

# ============================================
# GITHUB APP (para integração com repositórios)
# ============================================

# Crie um GitHub App em: https://github.com/settings/apps
# Permissões necessárias: Contents (read/write), Pull requests (read/write)
GITHUB_APP_ID=your_github_app_id
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nYour private key here\n-----END RSA PRIVATE KEY-----"

# ============================================
# VERCEL AUTH (opcional - para login via Vercel)
# ============================================

# Se você quiser autenticação via Vercel, configure:
# AUTH_VERCEL_ID=your_vercel_client_id
# AUTH_VERCEL_SECRET=your_vercel_client_secret

# ============================================
# DATABASE (PostgreSQL)
# ============================================

# URL de conexão com PostgreSQL
# Formato: postgresql://username:password@host:port/database
# Exemplo local: postgresql://postgres:postgres@localhost:5432/coding_agent
# Exemplo Neon: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/coding_agent

# ============================================
# SESSION
# ============================================

# Chave secreta para assinar cookies de sessão
# Gere uma chave aleatória com: openssl rand -base64 32
SESSION_SECRET=your_random_secret_key_minimum_32_characters

# ============================================
# OPCIONAL: Configurações adicionais
# ============================================

# Habilitar modo debug (true/false)
# DEBUG=false

# URL base do frontend (para callbacks)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# NOTAS DE CONFIGURAÇÃO
# ============================================

# 1. Copie este arquivo para .env.local:
#    cp .env.example .env.local

# 2. Preencha as variáveis com seus valores reais

# 3. NUNCA commit o arquivo .env.local no Git!
#    (já está no .gitignore)

# 4. Para produção, configure as variáveis no Vercel:
#    https://vercel.com/docs/environment-variables

# 5. Para rodar localmente, você precisa ter:
#    - Backend Atomic Agents rodando em http://localhost:8000
#    - PostgreSQL rodando localmente ou no Neon
#    - GitHub OAuth App configurado
#    - GitHub App configurado (para repos)

# 6. Consulte INTEGRATION.md para mais detalhes
